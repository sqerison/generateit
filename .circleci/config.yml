version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: env
      - run:
          name: Print the Current Time
          command: |
	    apt-get update ; apt-get install curl -y; \
            curl https://api.github.com/repos/sqerison/generateit/merges
